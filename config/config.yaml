database:
  host: ${DB_HOST}
  port: ${DB_PORT}
  name: ${DB_NAME}
  user: ${DB_USER}
  password: ${DB_PASSWORD}

data_generation:
  customers: 1000
  products: 500
  transactions: 10000
  start_date: "2025-01-01"
  end_date: "2025-12-31"

pipeline:
  batch_size: 1000
  log_level: INFO
  retries: 3

bi_tool:
  tool: tableau
